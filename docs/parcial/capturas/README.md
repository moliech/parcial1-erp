# 📸 Carpeta de Capturas de Pantalla Obligatorias

Guarda en esta carpeta las siguientes 4 imágenes antes de subir el proyecto a GitHub:

1. `mysql_tables.png`: Captura de pantalla de `SHOW TABLES;` en MySQL.
2. `migrations_code.png`: Captura de pantalla del código de las migraciones en VS Code.
3. `models_code.png`: Captura de pantalla del código de los modelos Eloquent (`Client.php`, `Pet.php`).
4. `seeder_inserted.png`: Captura de los 5 clientes insertados vistos en Tinker (`App\Models\Client::all()`) o MySQL.
